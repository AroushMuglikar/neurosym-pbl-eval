# Data Directory

Raw dataset files go here. 
**DO NOT COMMIT RAW DATA TO GITHUB.** 

Ensure `.gitignore` is set up to block `*.csv`, `*.mp4`, and `*.edf` files.

Currently staging this directory to receive the parsed outputs from:
1. SOPHIAS (presentation multimodal logs)
2. PE-HRI (temporal collaboration baseline logs)
